---
phase: 08-auditoria-de-seguranca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .clone/worktrees/agent-a8a1446e/packages/shared/src/types/game-state.ts
  - .clone/worktrees/agent-a8c8cb15/vitest.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Nenhum arquivo .clone/ esta rastreado pelo git"
    - ".gitignore cobre todos os padroes do template oficial Node/Next.js"
    - ".env.example contem todas as variaveis referenciadas no codigo"
    - "Todos os package.json tem private: true"
  artifacts:
    - path: ".gitignore"
      provides: "Protecao completa contra commit acidental de arquivos sensiveis"
      contains: "!.env.example"
    - path: ".env.example"
      provides: "Documentacao de todas as variaveis de ambiente necessarias"
      contains: "FRONTEND_URL"
  key_links:
    - from: ".gitignore"
      to: ".env.*"
      via: "glob pattern com excecao para .env.example"
      pattern: "\\.env\\.\\*"
---

<objective>
Limpeza imediata do repositorio: remover arquivos de worktree acidentalmente rastreados, atualizar .gitignore com todos os padroes do template oficial Node/Next.js, e verificar completude dos .env.example e private:true em todos os package.json.

Purpose: Eliminar todos os artefatos indesejados e lacunas de protecao no .gitignore antes de tornar o repositorio publico.
Output: Repositorio limpo com .gitignore completo, sem arquivos .clone/ rastreados, .env.example verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auditoria-de-seguran-a-e-limpeza-do-reposit-rio-para-torn-lo/08-RESEARCH.md
@.gitignore
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover arquivos .clone/ do tracking e atualizar .gitignore</name>
  <files>.gitignore, .clone/worktrees/agent-a8a1446e/packages/shared/src/types/game-state.ts, .clone/worktrees/agent-a8c8cb15/vitest.config.ts</files>
  <read_first>
    - .gitignore (estado atual - 8 linhas, falta muitos padroes)
    - .planning/phases/08-auditoria-de-seguran-a-e-limpeza-do-reposit-rio-para-torn-lo/08-RESEARCH.md (secao "Padroes de Arquitetura" contem o .gitignore recomendado completo)
  </read_first>
  <action>
1. Remover os dois arquivos .clone/ do tracking git SEM deletar do disco:
   ```bash
   git rm --cached ".clone/worktrees/agent-a8a1446e/packages/shared/src/types/game-state.ts"
   git rm --cached ".clone/worktrees/agent-a8c8cb15/vitest.config.ts"
   ```
   Nota: se os arquivos nao existirem no disco, usar `git rm --cached --ignore-unmatch`.

2. Substituir o .gitignore atual pelo conteudo completo baseado no template oficial Node/Next.js. O .gitignore atual tem `.claude` como entrada — verificar se `.clone` tambem precisa estar (a RESEARCH indica que `.clone/` ja esta, mas o arquivo real mostra `.claude`). Manter AMBOS: `.claude/` e `.clone/` como entradas separadas.

   Conteudo final do .gitignore (ordem por categoria, com comentarios em ingles para consistencia com ecosistema):

   ```
   # Dependencies
   node_modules/

   # Build output
   dist/
   .next/
   out/

   # Environment — never commit real values
   .env
   .env.*
   !.env.example

   # Test coverage
   coverage/
   .nyc_output/

   # TypeScript cache
   *.tsbuildinfo

   # Development tool artifacts
   .clone/
   .claude/

   # Logs
   *.log
   npm-debug.log*
   yarn-debug.log*
   yarn-error.log*

   # OS files
   .DS_Store
   Thumbs.db
   ehthumbs.db

   # Runtime
   pids/
   *.pid
   *.seed
   *.pid.lock

   # npm
   *.tgz
   .npm
   ```

3. Verificar com `git ls-files | grep ".clone"` que nenhum arquivo .clone/ esta rastreado apos a remocao.

4. Verificar que `.env.example` continua rastreado apos a mudanca de `.env.*` + `!.env.example`:
   ```bash
   git ls-files | grep "env.example"
   ```
   Deve retornar `.env.example`.
  </action>
  <verify>
    <automated>cd "C:/Users/julia/OneDrive/Documentos/GitHub/Coup" && git ls-files | grep ".clone" | wc -l | tr -d ' ' && echo "=== expect 0 ===" && git ls-files | grep "env.example" && echo "=== expect .env.example ===" && grep "!.env.example" .gitignore && echo "=== expect negation pattern ==="</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep ".clone"` retorna zero linhas
    - `git ls-files | grep "env.example"` retorna `.env.example`
    - `.gitignore` contem a linha `!.env.example`
    - `.gitignore` contem a linha `.env.*`
    - `.gitignore` contem a linha `*.log`
    - `.gitignore` contem a linha `.DS_Store`
    - `.gitignore` contem a linha `.clone/`
    - `.gitignore` contem a linha `.claude/`
    - `.gitignore` contem a linha `out/`
    - `.gitignore` contem a linha `.npm`
  </acceptance_criteria>
  <done>Arquivos .clone/ removidos do tracking, .gitignore atualizado com todos os padroes do template oficial Node/Next.js, .env.example continua rastreado.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar completude de .env.example e private:true em package.json</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (verificar se contem FRONTEND_URL, PORT, NODE_ENV, NEXT_PUBLIC_BACKEND_URL)
    - apps/backend/src/index.ts (source of truth para variaveis do backend)
    - apps/frontend/src/lib/backend-url.ts (source of truth para variaveis do frontend)
    - packages/shared/package.json (verificar private: true)
    - apps/backend/package.json (verificar private: true)
    - apps/frontend/package.json (verificar private: true)
    - package.json (verificar private: true)
  </read_first>
  <action>
1. Verificar que `.env.example` na raiz contem TODAS as variaveis referenciadas no codigo:
   - `FRONTEND_URL` (usado em `apps/backend/src/index.ts` e `apps/backend/src/cors-origins.ts`)
   - `PORT` (usado em `apps/backend/src/index.ts` — injetada pelo Railway)
   - `NODE_ENV` (padrao Node.js)
   - `NEXT_PUBLIC_BACKEND_URL` (usado em `apps/frontend/src/lib/backend-url.ts`)

   A RESEARCH ja confirmou que o `.env.example` atual contem todos estes valores com placeholders adequados. Confirmar lendo o arquivo. Se alguma variavel estiver faltando, adicionar com valor placeholder e comentario explicativo.

2. Verificar `"private": true` em TODOS os 4 package.json:
   - `package.json` (raiz)
   - `apps/backend/package.json`
   - `apps/frontend/package.json`
   - `packages/shared/package.json`

   A RESEARCH confirmou os 3 primeiros e assumiu o shared. O shared JA foi verificado e tem `private: true`.

3. Se qualquer package.json NAO tiver `private: true`, adicionar. Nenhuma mudanca esperada baseado na verificacao.

4. Fazer um grep completo por `process.env` em todo o codebase para confirmar que nao ha variaveis nao documentadas:
   ```bash
   grep -r "process.env" apps/ packages/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v ".next"
   ```
   Comparar resultado com as variaveis no .env.example.
  </action>
  <verify>
    <automated>cd "C:/Users/julia/OneDrive/Documentos/GitHub/Coup" && grep "FRONTEND_URL" .env.example > /dev/null && echo "FRONTEND_URL OK" && grep "NEXT_PUBLIC_BACKEND_URL" .env.example > /dev/null && echo "NEXT_PUBLIC OK" && grep '"private"' package.json apps/backend/package.json apps/frontend/package.json packages/shared/package.json | wc -l | tr -d ' ' && echo "=== expect 4 ==="</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contem `FRONTEND_URL=`
    - `.env.example` contem `NEXT_PUBLIC_BACKEND_URL=`
    - `grep '"private"' package.json` retorna `"private": true` para todos os 4 package.json
    - `grep -r "process.env" apps/ packages/ --include="*.ts"` nao revela nenhuma variavel nao documentada em .env.example
  </acceptance_criteria>
  <done>Todas as variaveis de ambiente do codigo estao documentadas no .env.example com placeholders. Todos os 4 package.json tem private: true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositorio git -> GitHub publico | Tudo no historico git sera visivel publicamente |
| .gitignore -> filesystem | Arquivos nao cobertos pelo .gitignore podem ser commitados acidentalmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | .clone/worktrees/ tracked files | mitigate | `git rm --cached` para remover do tracking (Task 1) |
| T-08-02 | Information Disclosure | .gitignore incompleto | mitigate | Atualizar com template completo Node/Next.js incluindo .env.*, *.log, OS files (Task 1) |
| T-08-03 | Information Disclosure | .env.example com vars faltando | mitigate | Verificar completude contra grep de process.env no codebase (Task 2) |
| T-08-04 | Tampering | package.json sem private:true | mitigate | Verificar todos os 4 package.json (Task 2) |
| T-08-05 | Information Disclosure | NEXT_PUBLIC_BACKEND_URL exposta no bundle | accept | Por design — URL de API publica, nao e segredo (documentado na RESEARCH) |
</threat_model>

<verification>
1. `git ls-files | grep ".clone"` retorna zero linhas
2. `git ls-files | grep "env.example"` retorna `.env.example`
3. `.gitignore` contem pelo menos 20 linhas com padroes completos
4. Todos os 4 package.json tem `"private": true`
5. Nenhuma variavel `process.env` no codigo sem correspondencia no .env.example
</verification>

<success_criteria>
- Repositorio limpo: zero arquivos .clone/ rastreados
- .gitignore completo: cobre dependencias, build, env, logs, OS, runtime, npm
- .env.example completo: todas as variaveis do codigo documentadas com placeholders
- Todos os packages marcados como privados (nao publicaveis no npm)
</success_criteria>

<output>
After completion, create `.planning/phases/08-auditoria-de-seguran-a-e-limpeza-do-reposit-rio-para-torn-lo/08-01-SUMMARY.md`
</output>
