---
phase: 06.1-ui-redesign-visual-game-interface
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - apps/frontend/src/components/game-board.tsx
  - apps/frontend/src/components/player-panel.tsx
  - apps/frontend/src/components/game-log.tsx
  - apps/frontend/src/components/winner-overlay.tsx
  - apps/frontend/src/components/connection-badge.tsx
  - apps/frontend/src/components/influence-card-selector.tsx
  - apps/frontend/src/components/exchange-selector.tsx
  - apps/frontend/src/components/coup-target-selector.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Game board uses dark zinc-950 header and zinc-900 card backgrounds"
    - "Player panel renders PlayerSeat components instead of flat div rows"
    - "Minhas Cartas section uses CharacterCard instead of Badge"
    - "GameLog fills available height on desktop instead of fixed max-h-[400px]"
    - "Winner overlay has Trophy icon and dark styling"
    - "InfluenceCardSelector shows CharacterCard inside each button"
    - "ExchangeSelector shows CharacterCard with selected/unselected states"
    - "CoupTargetSelector uses dark seat-style target buttons"
    - "ConnectionBadge is not fixed-bottom (avoids overlap with action bar)"
  artifacts:
    - path: "apps/frontend/src/components/game-board.tsx"
      provides: "Restyled game layout with CharacterCard in Minhas Cartas"
      contains: "CharacterCard"
    - path: "apps/frontend/src/components/player-panel.tsx"
      provides: "Player list using PlayerSeat components"
      contains: "PlayerSeat"
    - path: "apps/frontend/src/components/winner-overlay.tsx"
      provides: "Restyled winner screen"
      contains: "Trophy"
  key_links:
    - from: "game-board.tsx"
      to: "character-card.tsx"
      via: "CharacterCard import for Minhas Cartas"
      pattern: "import.*CharacterCard.*from.*character-card"
    - from: "player-panel.tsx"
      to: "player-seat.tsx"
      via: "PlayerSeat import for player list"
      pattern: "import.*PlayerSeat.*from.*player-seat"
---

<objective>
Restyle the game board layout and all non-action-bar game components to the dark card-game aesthetic. Wire CharacterCard and PlayerSeat from Plan 01 into their consumers.

Purpose: Transform the game view from plain shadcn to the conspiracy-themed dark UI. This covers the main game layout, player panel, game log, winner overlay, card selectors, and target selector.
Output: 8 restyled component files, all using the new design tokens and components.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-CONTEXT.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-RESEARCH.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — new components to consume -->

From apps/frontend/src/components/character-card.tsx:
```typescript
export const CHARACTER_CONFIG: Record<string, {
  label: string
  colorClass: string
  borderClass: string
  Icon: React.ComponentType<{ className?: string }>
}>

interface CharacterCardProps {
  type: CardType | string
  revealed: boolean
  showFace?: boolean   // default true
  size?: "sm" | "md" | "lg"  // default "md"
  className?: string
}

export function CharacterCard({ type, revealed, showFace, size, className }: CharacterCardProps)
```

From apps/frontend/src/components/player-seat.tsx:
```typescript
interface PlayerSeatProps {
  player: PublicPlayerState
  isActive: boolean
  isMe: boolean
  isDisconnected: boolean
}

export function PlayerSeat({ player, isActive, isMe, isDisconnected }: PlayerSeatProps)
```

From packages/shared/src/types/card.ts:
```typescript
export interface Card { type: CardType; revealed: boolean }
```

From packages/shared/src/types/game-state.ts:
```typescript
export interface PublicPlayerState {
  id: string; name: string; coins: number; eliminated: boolean;
  cardCount: number; revealedCards: Card[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameBoard layout + Minhas Cartas + PlayerPanel + GameLog reskin</name>
  <files>
    apps/frontend/src/components/game-board.tsx,
    apps/frontend/src/components/player-panel.tsx,
    apps/frontend/src/components/game-log.tsx,
    apps/frontend/src/components/connection-badge.tsx
  </files>
  <read_first>
    apps/frontend/src/components/game-board.tsx,
    apps/frontend/src/components/player-panel.tsx,
    apps/frontend/src/components/game-log.tsx,
    apps/frontend/src/components/connection-badge.tsx,
    apps/frontend/src/components/character-card.tsx,
    apps/frontend/src/components/player-seat.tsx
  </read_first>
  <action>
**game-board.tsx** — restyle layout, wire CharacterCard for "Minhas Cartas":
1. Add import: `import { CharacterCard } from "@/components/character-card"`
2. Header: change to `bg-zinc-950 border-b border-zinc-800` (instead of `border-border`). Keep h-14 px-6 layout.
3. "Minhas Cartas" section: replace Badge-based card rendering with CharacterCard:
   ```tsx
   {game.myHand.map((card, idx) => (
     <CharacterCard
       key={idx}
       type={card.type}
       revealed={card.revealed}
       showFace={true}
       size="sm"
     />
   ))}
   ```
   Remove Badge import for this section (keep Badge if used elsewhere). The UICard wrapper for "Minhas Cartas" stays but add `bg-zinc-900/50 border-zinc-800` classes.
4. Main content area: keep `flex-1 flex flex-col md:flex-row gap-4 p-4`. The left panel UICard (containing PlayerPanel) should have `bg-zinc-900/50 border-zinc-800`.
5. GameLog right panel: on desktop, use `flex-1 flex flex-col` with min-h-0 so GameLog fills height. On mobile, keep toggle behavior unchanged.
6. Outer div: change from `pb-16` to `pb-20` (more space for styled action bar).
7. Do NOT change any logic, state, conditionals, or socket emissions. Only className and CharacterCard import changes.

**player-panel.tsx** — wire PlayerSeat:
1. Add import: `import { PlayerSeat } from "@/components/player-seat"`
2. Replace the `players.map()` inner content: each player row becomes:
   ```tsx
   <PlayerSeat
     key={player.id}
     player={player}
     isActive={player.id === activePlayerId}
     isMe={player.id === myId}
     isDisconnected={disconnectedPlayers.includes(player.id)}
   />
   ```
3. Remove the existing `Badge` imports and inline div structure for player rows.
4. Keep the outer Card/CardHeader/CardContent wrapper but update: `<Card className="bg-zinc-900/50 border-zinc-800">`, `<CardTitle className="text-base text-zinc-300">Jogadores</CardTitle>`.
5. Props interface stays IDENTICAL.

**game-log.tsx** — minor restyle:
1. Outer Card: add `bg-zinc-900/50 border-zinc-800`.
2. CardTitle: `text-base text-zinc-300` (instead of `text-lg`).
3. Replace `max-h-[400px]` with `h-0 flex-1` on the inner scrollable div to fill available space in the parent flex container. Keep `overflow-y-auto`.
4. Log entries: add `text-zinc-400` for regular entries. Keep `text-sm`.
5. Props interface stays IDENTICAL.

**connection-badge.tsx** — position fix:
1. Remove `fixed bottom-4 right-4 z-50` from the outer div.
2. Replace with `flex items-center gap-1.5 px-2 py-1 rounded-md` (no positioning — it sits inside the header in GameBoard). The GameBoard header already renders `<ConnectionBadge />` inline.
3. Home page and lobby page render ConnectionBadge differently (inline in their layouts) — this change works for all contexts since it becomes a flow element.
4. Keep all status logic unchanged.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - game-board.tsx contains `import.*CharacterCard.*from.*character-card`
    - game-board.tsx contains `<CharacterCard` in the Minhas Cartas section
    - game-board.tsx contains `bg-zinc-950` in header
    - player-panel.tsx contains `import.*PlayerSeat.*from.*player-seat`
    - player-panel.tsx contains `<PlayerSeat` inside the map
    - game-log.tsx does NOT contain `max-h-\[400px\]`
    - game-log.tsx contains `bg-zinc-900`
    - connection-badge.tsx does NOT contain `fixed bottom-4`
    - connection-badge.tsx does NOT contain `z-50`
    - TypeScript compiles with no errors
    - All existing tests pass
  </acceptance_criteria>
  <done>
    GameBoard uses CharacterCard for Minhas Cartas section. PlayerPanel renders PlayerSeat components. GameLog fills available height. ConnectionBadge is inline (not fixed-bottom). All use dark zinc backgrounds. No logic changes. TypeScript and tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: WinnerOverlay + InfluenceCardSelector + ExchangeSelector + CoupTargetSelector reskin</name>
  <files>
    apps/frontend/src/components/winner-overlay.tsx,
    apps/frontend/src/components/influence-card-selector.tsx,
    apps/frontend/src/components/exchange-selector.tsx,
    apps/frontend/src/components/coup-target-selector.tsx
  </files>
  <read_first>
    apps/frontend/src/components/winner-overlay.tsx,
    apps/frontend/src/components/influence-card-selector.tsx,
    apps/frontend/src/components/exchange-selector.tsx,
    apps/frontend/src/components/coup-target-selector.tsx,
    apps/frontend/src/components/character-card.tsx
  </read_first>
  <action>
**winner-overlay.tsx** — trophy icon + dark styling:
1. Add import: `import { Trophy } from "lucide-react"`
2. Outer overlay: keep `fixed inset-0 z-50 flex items-center justify-center`, change backdrop to `bg-zinc-950/95 backdrop-blur-md`.
3. Inner Card: add `bg-zinc-900 border-zinc-700 shadow-2xl`.
4. Add `<Trophy className="h-12 w-12 text-yellow-400 mx-auto mb-2" />` above the "Fim de Jogo" heading.
5. Winner name: add `text-zinc-100` for contrast.
6. "Vencedor!" label: keep `text-muted-foreground`.
7. Revanche button: keep `variant="default"`, add `min-h-[44px]`.
8. Do NOT change socket.emit or any logic.

**influence-card-selector.tsx** — use CharacterCard:
1. Add import: `import { CharacterCard } from "@/components/character-card"`
2. Outer container: change to `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`.
3. Each card button: wrap a `<CharacterCard type={card.type} revealed={false} showFace={true} size="sm" />` inside the Button, alongside the text "Perder esta".
4. Button styling: `variant="ghost"` with `border border-destructive/40 hover:bg-destructive/10 min-h-[44px]`.
5. Keep socket.emit and index logic identical.

**exchange-selector.tsx** — use CharacterCard:
1. Add import: `import { CharacterCard } from "@/components/character-card"`
2. Outer container: `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`.
3. Replace Badge-based card display: each card becomes a clickable div wrapping `<CharacterCard type={card.type} revealed={false} showFace={true} size="md" />`.
4. Selected state: `ring-2 ring-primary` on the wrapper div. Unselected: `opacity-50`.
5. Remove Badge import. Keep `useState` selection logic identical.
6. Confirm button: keep existing logic, add `min-h-[44px]`.

**coup-target-selector.tsx** — dark seat-style targets:
1. Outer container: `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`.
2. Target buttons: change from `ring-2 ring-destructive` to `bg-zinc-900 border border-zinc-700 hover:border-destructive hover:bg-zinc-800`. Keep avatar initial circle.
3. Avatar circle: `bg-zinc-800 text-zinc-300` instead of `bg-muted`.
4. Cancel button: `variant="ghost"` with `text-zinc-500 hover:text-zinc-300`.
5. Keep all socket.emit and target selection logic identical.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - winner-overlay.tsx contains `import.*Trophy.*from.*lucide-react`
    - winner-overlay.tsx contains `<Trophy`
    - winner-overlay.tsx contains `bg-zinc-950` or `bg-zinc-900`
    - influence-card-selector.tsx contains `import.*CharacterCard`
    - influence-card-selector.tsx contains `<CharacterCard`
    - exchange-selector.tsx contains `import.*CharacterCard`
    - exchange-selector.tsx contains `<CharacterCard`
    - exchange-selector.tsx does NOT contain `<Badge` (replaced by CharacterCard)
    - coup-target-selector.tsx contains `bg-zinc-900`
    - TypeScript compiles with no errors
    - All existing tests pass
  </acceptance_criteria>
  <done>
    WinnerOverlay has Trophy icon and dark card styling. InfluenceCardSelector and ExchangeSelector use CharacterCard with character colors. CoupTargetSelector has dark seat-style buttons. No logic changes. TypeScript and tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Purely presentational changes — no new data flows, no user input handling changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-02 | I (Info Disclosure) | CharacterCard in selectors | accept | Card data comes from server-projected state; player only sees cards they are entitled to see per game rules (own hand, exchange cards) |
</threat_model>

<verification>
1. `cd apps/frontend && npx vitest run` — all tests pass
2. `cd apps/frontend && npx tsc --noEmit` — TypeScript compiles clean
3. `grep "CharacterCard" apps/frontend/src/components/game-board.tsx` — imported and used
4. `grep "PlayerSeat" apps/frontend/src/components/player-panel.tsx` — imported and used
5. `grep "Trophy" apps/frontend/src/components/winner-overlay.tsx` — icon imported and rendered
</verification>

<success_criteria>
- GameBoard layout uses dark zinc backgrounds, CharacterCard in Minhas Cartas
- PlayerPanel renders PlayerSeat components (not flat divs)
- GameLog fills height dynamically (no hardcoded max-h)
- ConnectionBadge is inline, not fixed-bottom
- WinnerOverlay has Trophy icon and dark styling
- Card selectors (influence, exchange) use CharacterCard
- CoupTargetSelector has dark styled buttons
- All existing tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-02-SUMMARY.md`
</output>
