---
phase: 06.1-ui-redesign-visual-game-interface
plan: "01"
subsystem: ui
tags: [react, tailwind, lucide-react, character-card, player-seat, css-tokens]

requires:
  - phase: 04-basic-game-loop
    provides: PublicPlayerState type, CardType enum from @coup/shared

provides:
  - 5 character color CSS tokens in globals.css @theme block (duke/captain/assassin/ambassador/countess)
  - CharacterCard component with face-up, face-down, and revealed/eliminated states
  - CHARACTER_CONFIG export mapping each CardType to label, colorClass, borderClass, and icon
  - PlayerSeat component with avatar, coins/cards display, active ring, eliminated styling, and revealed cards

affects:
  - 06.1-02
  - 06.1-03

tech-stack:
  added: []
  patterns:
    - "Tailwind v4 @theme tokens for character colors — --color-character-duke → bg-character-duke, text-character-duke, border-character-duke"
    - "CHARACTER_CONFIG record pattern for mapping CardType enum to visual config (label, colorClass, borderClass, Icon)"
    - "cn() for conditional class composition in all new components"
    - "TDD cycle: write failing tests first, then implement to pass"

key-files:
  created:
    - apps/frontend/src/components/character-card.tsx
    - apps/frontend/src/components/player-seat.tsx
    - apps/frontend/src/components/__tests__/character-card.test.tsx
    - apps/frontend/src/components/__tests__/player-seat.test.tsx
  modified:
    - apps/frontend/src/app/globals.css

key-decisions:
  - "CHARACTER_CONFIG exported as named export so Plans 02/03 can import icon+color config without duplicating mappings"
  - "showFace prop added to CharacterCard beyond plan spec to clearly separate face visibility from revealed/eliminated state"
  - "PlayerSeat test mocks CharacterCard as a simple div for isolation — avoids Lucide icon rendering complexity in tests"

patterns-established:
  - "Character color token naming: --color-character-{name} in globals.css maps to bg/text/border-character-{name} Tailwind classes"
  - "CharacterCard revealed=true means eliminated/greyed-out, showFace=false means card back (hidden from opponent)"

requirements-completed: []

duration: 12min
completed: 2026-04-07
---

# Phase 06.1 Plan 01: Design Foundation Summary

**CharacterCard and PlayerSeat visual building blocks with 5 Tailwind v4 character color tokens, 16 passing unit tests**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-04-07T09:00:00Z
- **Completed:** 2026-04-07T09:12:00Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added 5 character color CSS custom properties to globals.css @theme block, making `bg-character-duke`, `text-character-captain`, etc. available as Tailwind utility classes
- Created CharacterCard component handling face-up (with character color/icon/label), face-down (card back with "?" and Shield icon), and revealed/eliminated (opacity-40 grayscale) states across three sizes (sm/md/lg)
- Created PlayerSeat component showing avatar initial, coin count with Coins icon, card count with Shield icon, active ring-2 highlight, eliminated opacity-40 + Eliminado badge, and a row of revealed CharacterCards
- 16 unit tests across 2 test files all passing with no regressions in the existing 33-test suite

## Task Commits

Each task was committed atomically:

1. **Task 1: Character color tokens + CharacterCard component** - `b5e483f` (feat)
2. **Task 2: PlayerSeat component** - `4d622a2` (feat)

_Note: Both tasks followed TDD cycle (RED then GREEN)_

## Files Created/Modified

- `apps/frontend/src/app/globals.css` - Added 5 character color @theme tokens
- `apps/frontend/src/components/character-card.tsx` - New CharacterCard component + CHARACTER_CONFIG export
- `apps/frontend/src/components/player-seat.tsx` - New PlayerSeat component
- `apps/frontend/src/components/__tests__/character-card.test.tsx` - 8 unit tests for CharacterCard
- `apps/frontend/src/components/__tests__/player-seat.test.tsx` - 8 unit tests for PlayerSeat

## Decisions Made

- `showFace` prop added beyond plan spec to clearly separate face visibility (owner sees their own cards) from `revealed` (eliminated/greyed-out state) — this matches the semantic clarification in RESEARCH.md Pitfall 2
- `CHARACTER_CONFIG` exported as a named export (per plan spec) so Plans 02 and 03 can import icon/color mappings without duplicating them
- PlayerSeat test mocks CharacterCard as a simple div for test isolation — avoids Lucide SVG rendering complexity

## Deviations from Plan

None - plan executed exactly as written. The `showFace` prop addition was already specified in the plan's action section.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CharacterCard and PlayerSeat are ready for consumption by Plans 02 and 03
- CHARACTER_CONFIG export enables Plan 03 (ActionBar) to use character colors and icons for action buttons
- All 5 character color tokens available as Tailwind utilities for any component in the frontend

## Self-Check: PASSED

- FOUND: apps/frontend/src/app/globals.css
- FOUND: apps/frontend/src/components/character-card.tsx
- FOUND: apps/frontend/src/components/player-seat.tsx
- FOUND: apps/frontend/src/components/__tests__/character-card.test.tsx
- FOUND: apps/frontend/src/components/__tests__/player-seat.test.tsx
- FOUND commit: b5e483f (Task 1)
- FOUND commit: 4d622a2 (Task 2)

---
*Phase: 06.1-ui-redesign-visual-game-interface*
*Completed: 2026-04-07*
