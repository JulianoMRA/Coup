---
phase: 06.1-ui-redesign-visual-game-interface
plan: 03
type: execute
wave: 3
depends_on: ["06.1-01"]
files_modified:
  - apps/frontend/src/components/action-bar.tsx
  - apps/frontend/src/components/reaction-bar.tsx
  - apps/frontend/src/components/block-challenge-bar.tsx
  - apps/frontend/src/components/block-claim-selector.tsx
  - apps/frontend/src/app/page.tsx
  - apps/frontend/src/app/room/[roomId]/page.tsx
autonomous: false

requirements: []

must_haves:
  truths:
    - "Action bar buttons show character-specific colors and icons (Duke=purple/Crown, Captain=blue/Anchor, Assassin=red/Sword, Ambassador=green/Globe)"
    - "Forced coup notice is visually prominent with destructive styling"
    - "Reaction bar and block-challenge bar use dark zinc backgrounds consistent with game board"
    - "Block claim selector buttons show character colors and icons"
    - "Home screen has dark aesthetic with game title treatment"
    - "Lobby screen has dark cards, avatar circles, ready badges consistent with game design"
    - "All interactive buttons have min-h-[44px] touch targets"
  artifacts:
    - path: "apps/frontend/src/components/action-bar.tsx"
      provides: "Character-colored action buttons with icons"
      contains: "Crown"
    - path: "apps/frontend/src/app/page.tsx"
      provides: "Restyled home screen"
      contains: "bg-zinc-950"
    - path: "apps/frontend/src/app/room/[roomId]/page.tsx"
      provides: "Restyled lobby screen"
      contains: "bg-zinc-900"
  key_links:
    - from: "action-bar.tsx"
      to: "character-card.tsx"
      via: "CHARACTER_CONFIG import for icon/color lookup"
      pattern: "import.*CHARACTER_CONFIG.*from.*character-card"
    - from: "block-claim-selector.tsx"
      to: "character-card.tsx"
      via: "CHARACTER_CONFIG import for icon/color lookup"
      pattern: "import.*CHARACTER_CONFIG.*from.*character-card"
---

<objective>
Restyle the action bars (ActionBar, ReactionBar, BlockChallengeBar, BlockClaimSelector) with character colors and icons, and polish the Home and Lobby screens to match the dark game aesthetic.

Purpose: Complete the visual redesign by applying character-themed colors to all interactive bottom bars and ensuring the entry screens (home + lobby) are visually consistent with the game board.
Output: 6 restyled files covering all bottom bars and both page-level screens.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-CONTEXT.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-RESEARCH.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-01-SUMMARY.md

<interfaces>
<!-- CHARACTER_CONFIG from Plan 01 — used for icon/color on action buttons -->

From apps/frontend/src/components/character-card.tsx:
```typescript
export const CHARACTER_CONFIG: Record<string, {
  label: string
  colorClass: string      // e.g. "text-character-duke"
  borderClass: string     // e.g. "border-character-duke"
  Icon: React.ComponentType<{ className?: string }>
}>
// Keys: DUKE, CAPTAIN, ASSASSIN, AMBASSADOR, CONTESSA
```

Character-to-action mapping:
- TAX = Duke (purple, Crown)
- STEAL = Captain (blue, Anchor)
- ASSASSINATE = Assassin (red, Sword)
- EXCHANGE = Ambassador (green, Globe)
- BLOCK with CONTESSA = Countess (pink, Gem)
- BLOCK with DUKE = Duke (purple, Crown)
- BLOCK with CAPTAIN = Captain (blue, Anchor)
- BLOCK with AMBASSADOR = Ambassador (green, Globe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActionBar + ReactionBar + BlockChallengeBar + BlockClaimSelector character-colored reskin</name>
  <files>
    apps/frontend/src/components/action-bar.tsx,
    apps/frontend/src/components/reaction-bar.tsx,
    apps/frontend/src/components/block-challenge-bar.tsx,
    apps/frontend/src/components/block-claim-selector.tsx
  </files>
  <read_first>
    apps/frontend/src/components/action-bar.tsx,
    apps/frontend/src/components/reaction-bar.tsx,
    apps/frontend/src/components/block-challenge-bar.tsx,
    apps/frontend/src/components/block-claim-selector.tsx,
    apps/frontend/src/components/character-card.tsx
  </read_first>
  <action>
**action-bar.tsx** — character-colored buttons with icons:
1. Add imports: `import { Crown, Anchor, Sword, Globe, Coins as CoinsIcon } from "lucide-react"` and `import { cn } from "@/lib/utils"`
2. Outer container: change to `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`. Keep `fixed bottom-0 left-0 right-0`.
3. Forced coup notice: wrap in a `bg-destructive/10 border border-destructive/30 rounded-md px-3 py-1.5` container for prominence. Keep text content.
4. Row 1 (basic actions) — use grid for mobile: `grid grid-cols-3 gap-2 w-full max-w-lg mx-auto`:
   - Renda: `variant="ghost"` with `cn("min-h-[44px] border border-zinc-700 hover:bg-zinc-800", (!canAct || forcedCoup) && "opacity-40")`. Add `<CoinsIcon className="h-4 w-4 mr-1" />` before text.
   - Ajuda Externa: same ghost+border pattern, `border-zinc-700`.
   - Golpe: `variant="ghost"` with `cn("min-h-[44px] border border-destructive/60 text-destructive hover:bg-destructive/10", (!canAct || !canCoup) && "opacity-40")`. Keep destructive styling.
5. Row 2 (character actions) — use grid for mobile: `grid grid-cols-2 sm:grid-cols-4 gap-2 w-full max-w-lg mx-auto`:
   - Imposto: `variant="ghost"` with `cn("min-h-[44px] gap-1.5 border border-character-duke/50 text-character-duke hover:bg-character-duke/10", (!canAct || forcedCoup) && "opacity-40")`. Prepend `<Crown className="h-4 w-4" />`. Label: "Imposto".
   - Roubar: `border-character-captain/50 text-character-captain hover:bg-character-captain/10`. Prepend `<Anchor className="h-4 w-4" />`. Label: "Roubar".
   - Assassinar: `border-character-assassin/50 text-character-assassin hover:bg-character-assassin/10`. Prepend `<Sword className="h-4 w-4" />`. Label: "Assassinar". Keep `myCoins < 3` disable.
   - Embaixador: `border-character-ambassador/50 text-character-ambassador hover:bg-character-ambassador/10`. Prepend `<Globe className="h-4 w-4" />`. Label: "Embaixador".
6. All buttons: add `min-h-[44px]` for touch targets.
7. Do NOT change any handler functions or socket.emit calls.

**reaction-bar.tsx** — dark footer + character colors:
1. Add import: `import { cn } from "@/lib/utils"`
2. Outer container: `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`. Keep fixed positioning.
3. Action announcement text: `text-sm text-zinc-300 font-medium text-center` (brighter than muted).
4. Contestar button: `variant="ghost"` with `border border-destructive/50 text-destructive hover:bg-destructive/10 min-h-[44px]`.
5. Bloquear button: `variant="ghost"` with `border border-zinc-600 hover:bg-zinc-800 min-h-[44px]`.
6. Passar button: `variant="ghost"` with `border border-zinc-700 text-zinc-400 hover:bg-zinc-800 min-h-[44px]`.
7. "Voce passou" text: `text-zinc-500` for dimmed appearance.
8. Do NOT change any handler functions or socket logic.

**block-challenge-bar.tsx** — dark footer:
1. Add import: `import { cn } from "@/lib/utils"`
2. Outer container: `bg-zinc-950/95 backdrop-blur-sm border-t border-zinc-800`.
3. Announcement text: `text-sm font-semibold text-zinc-200 text-center`.
4. Contestar bloco button: `variant="ghost"` with `border border-destructive/50 text-destructive hover:bg-destructive/10 min-h-[44px]`.
5. Aceitar bloco button: `variant="ghost"` with `border border-zinc-700 text-zinc-400 hover:bg-zinc-800 min-h-[44px]`.
6. Do NOT change any handler functions.

**block-claim-selector.tsx** — character-colored block buttons:
1. Add imports: `import { CHARACTER_CONFIG } from "@/components/character-card"` and `import { cn } from "@/lib/utils"`
2. Each card button: look up `CHARACTER_CONFIG[card]` for icon and color. Render:
   ```tsx
   <Button
     key={card}
     variant="ghost"
     className={cn(
       "min-h-[44px] gap-2 border",
       config.borderClass + "/50",
       config.colorClass,
       CARD_HOVER[card]
     )}
     onClick={...existing handler...}
   >
     <config.Icon className="h-4 w-4" />
     {CARD_LABELS[card] ?? card}
   </Button>
   ```
   NOTE: Because Tailwind needs full class names (no string interpolation for purging), construct the hover class using the `cn()` utility and full explicit classes. For each card type, use a small lookup:
   ```typescript
   const CARD_HOVER: Record<string, string> = {
     DUKE: "hover:bg-character-duke/10",
     CAPTAIN: "hover:bg-character-captain/10",
     AMBASSADOR: "hover:bg-character-ambassador/10",
     CONTESSA: "hover:bg-character-countess/10",
     ASSASSIN: "hover:bg-character-assassin/10",
   }
   ```
3. Voltar button: `variant="ghost"` with `text-zinc-500 hover:text-zinc-300`.
4. Do NOT change socket.emit or handler logic.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - action-bar.tsx contains `import.*Crown.*Anchor.*Sword.*Globe` from lucide-react
    - action-bar.tsx contains `character-duke` (character color class)
    - action-bar.tsx contains `character-captain` (character color class)
    - action-bar.tsx contains `character-assassin` (character color class)
    - action-bar.tsx contains `character-ambassador` (character color class)
    - action-bar.tsx contains `min-h-[44px]` on buttons
    - action-bar.tsx contains `bg-zinc-950` in the outer container
    - reaction-bar.tsx contains `bg-zinc-950`
    - block-challenge-bar.tsx contains `bg-zinc-950`
    - block-claim-selector.tsx contains `CHARACTER_CONFIG`
    - TypeScript compiles with no errors
    - All existing tests pass
  </acceptance_criteria>
  <done>
    ActionBar shows character-colored buttons with icons (Duke=purple/Crown, Captain=blue/Anchor, Assassin=red/Sword, Ambassador=green/Globe). All bottom bars use dark zinc-950 backgrounds. BlockClaimSelector buttons have character colors. All buttons have 44px touch targets. No logic changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Home screen + Lobby screen dark aesthetic</name>
  <files>
    apps/frontend/src/app/page.tsx,
    apps/frontend/src/app/room/[roomId]/page.tsx
  </files>
  <read_first>
    apps/frontend/src/app/page.tsx,
    apps/frontend/src/app/room/[roomId]/page.tsx
  </read_first>
  <action>
**page.tsx (Home screen)** — dark polished entry:
1. Add import: `import { Swords } from "lucide-react"` (thematic icon for Coup game title).
2. Main wrapper: keep `min-h-screen flex flex-col items-center justify-center p-8`. Background is already near-black from globals.css.
3. Content card: wrap the form in a `div` with `bg-zinc-900/60 border border-zinc-800 rounded-xl p-8 shadow-2xl`.
4. Title treatment: replace plain `<h1>` with a styled block:
   ```tsx
   <div className="flex items-center justify-center gap-3 mb-2">
     <Swords className="h-8 w-8 text-zinc-400" />
     <h1 className="text-3xl font-bold tracking-wider text-zinc-100">COUP</h1>
     <Swords className="h-8 w-8 text-zinc-400 -scale-x-100" />
   </div>
   <p className="text-sm text-zinc-500 text-center tracking-wide mb-4">Online</p>
   ```
5. Input: add `bg-zinc-800 border-zinc-700 text-zinc-100 placeholder:text-zinc-500` classes.
6. Button: keep `variant="default"`, add `min-h-[44px]`.
7. Help text: keep `text-muted-foreground`, adjust to `text-zinc-500`.
8. ConnectionBadge: it's now inline (not fixed), so remove any wrapping if needed. Keep it above the title or in a subtle corner.
9. Do NOT change any logic, router calls, or fetch calls.

**room/[roomId]/page.tsx (Lobby + join screen)** — dark aesthetic:
1. Add import: `import { Swords } from "lucide-react"` for consistent title icon.
2. **Username entry sub-state** (when `!playerName`):
   - Wrap form in `bg-zinc-900/60 border border-zinc-800 rounded-xl p-8 shadow-2xl`.
   - Title: `<h1 className="text-xl font-semibold text-zinc-100">Entrar na Sala</h1>`.
   - Room ID label: `text-zinc-500`.
   - Input: `bg-zinc-800 border-zinc-700 text-zinc-100 placeholder:text-zinc-500`.
   - Button: add `min-h-[44px]`.

3. **Room not found sub-state**: keep simple, add `text-zinc-300` to message.

4. **Active lobby sub-state**:
   - Main wrapper: keep `min-h-screen flex flex-col items-center py-8 px-4`.
   - Title area: keep layout, update `text-zinc-100` for heading.
   - Invite Card: add `bg-zinc-900/60 border-zinc-800`. Link text: `text-zinc-400 font-mono`. Copy button: keep `variant="secondary"`, add `bg-zinc-800 hover:bg-zinc-700 border-zinc-700`.
   - Players Card: add `bg-zinc-900/60 border-zinc-800`. CardTitle: `text-zinc-300`.
   - Player rows: avatar circles `bg-zinc-700 text-zinc-300`. Name text: `text-zinc-200`.
   - Ready Badge: keep emerald-500 for "Pronto", change "Aguardando" to `bg-zinc-800 text-zinc-500 border border-zinc-700`.
   - Ready toggle button: keep variants, add `min-h-[44px]`.
   - Start button: add `min-h-[44px]`.

5. Do NOT change any logic, socket emissions, or state management.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import.*Swords.*from.*lucide-react`
    - page.tsx contains `tracking-wider` (title letter spacing)
    - page.tsx contains `bg-zinc-900` (card background)
    - page.tsx contains `COUP` as displayed game title text
    - room/[roomId]/page.tsx contains `bg-zinc-900` (card backgrounds)
    - room/[roomId]/page.tsx contains `min-h-[44px]` on buttons
    - room/[roomId]/page.tsx contains `bg-zinc-700` (avatar circles)
    - TypeScript compiles with no errors
    - All existing tests pass
  </acceptance_criteria>
  <done>
    Home screen has dark card container, "COUP" title with Swords icons, styled input/button.
    Lobby screen has dark card backgrounds, styled avatars, consistent ready badge colors.
    Both screens are visually consistent with the game board aesthetic.
    No logic changes. TypeScript and tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete UI redesign</name>
  <files>N/A</files>
  <action>Human visually verifies the complete UI redesign across all screens and interaction states.</action>
  <verify>
    <automated>cd apps/frontend && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>User approves the visual redesign or provides specific issues to fix.</done>
  <what-built>
    Complete visual redesign of the Coup Online interface across all 3 plans:
    - Plan 01: Character color tokens + CharacterCard + PlayerSeat components
    - Plan 02: GameBoard layout, PlayerPanel, GameLog, WinnerOverlay, card selectors reskin
    - Plan 03: ActionBar with character colors/icons, ReactionBar, BlockChallengeBar, Home + Lobby pages

    The entire UI should now have a dark conspiracy-themed aesthetic with character-colored cards, player seat tiles, icon-enhanced buttons, and consistent zinc backgrounds.
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev` from repo root
    2. **Home screen** (http://localhost:3000):
       - Dark background with "COUP" title and Swords icons
       - Input and button have dark styling
       - No emojis anywhere
    3. **Create a room** and check **Lobby screen**:
       - Dark card backgrounds for invite link and player list
       - Avatar circles with initials
       - Ready/Aguardando badges match theme
       - Copy link button styled correctly
    4. **Start a game** (open 2nd browser tab for 2nd player):
       - Player seats show avatar + name + coin (yellow) + card count icons
       - Active player has white ring highlight
       - "Minhas Cartas" shows CharacterCard components (colored borders + icons, not plain text)
       - GameLog has dark background, fills height on desktop
       - Action buttons show character colors: Imposto (purple/Crown), Roubar (blue/Anchor), Assassinar (red/Sword), Embaixador (green/Globe)
       - Golpe button is destructive red
       - Forced coup notice is visually prominent when player has 10+ coins
    5. **Test reaction flow** (declare a character action):
       - Reaction bar has dark background with Contestar (red) and Passar buttons
       - Block claim buttons show character colors and icons
    6. **Test influence loss**: InfluenceCardSelector shows CharacterCard inside buttons
    7. **Test exchange**: ExchangeSelector shows CharacterCard with selection ring
    8. **Play to game over**: WinnerOverlay has Trophy icon, dark card, winner name
    9. **Mobile check** (resize to 375px width or use DevTools):
       - Single column layout, no horizontal overflow
       - Action buttons in 2-column grid, all tappable (44px+)
       - Game log toggle works (Ver Log / Ocultar Log)
       - Player seats compact but readable
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Purely presentational styling changes — no new inputs, no backend changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-03 | S (Spoofing) | Home/Lobby pages | accept | No auth changes; session model unchanged from Phase 1 |
</threat_model>

<verification>
1. `cd apps/frontend && npx vitest run` — all tests pass
2. `cd apps/frontend && npx tsc --noEmit` — TypeScript compiles clean
3. Visual inspection via checkpoint task
</verification>

<success_criteria>
- ActionBar buttons show character icons and colors (Crown/purple, Anchor/blue, Sword/red, Globe/green)
- All bottom bars use bg-zinc-950 dark backgrounds
- BlockClaimSelector buttons have per-character colors and icons
- Home screen has dark "COUP" title with Swords icons
- Lobby screen has dark cards, styled avatars, themed badges
- All buttons have 44px minimum touch targets
- Mobile layout works without horizontal overflow
- No logic, state, or socket changes in any file
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-03-SUMMARY.md`
</output>
