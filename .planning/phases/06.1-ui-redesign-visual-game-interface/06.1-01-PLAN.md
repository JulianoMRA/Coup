---
phase: 06.1-ui-redesign-visual-game-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/globals.css
  - apps/frontend/src/components/character-card.tsx
  - apps/frontend/src/components/player-seat.tsx
  - apps/frontend/src/components/__tests__/character-card.test.tsx
  - apps/frontend/src/components/__tests__/player-seat.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Character color tokens are available as Tailwind utility classes (bg-character-duke, text-character-captain, etc.)"
    - "CharacterCard renders face-up with correct color, icon, and label per character type"
    - "CharacterCard renders revealed/eliminated cards with greyed-out opacity treatment"
    - "PlayerSeat shows avatar initial, name, coin count with Coins icon, card count with Shield icon"
    - "PlayerSeat highlights active player with ring and dims eliminated players"
  artifacts:
    - path: "apps/frontend/src/app/globals.css"
      provides: "5 character color CSS custom properties under @theme"
      contains: "--color-character-duke"
    - path: "apps/frontend/src/components/character-card.tsx"
      provides: "Reusable CharacterCard component"
      exports: ["CharacterCard"]
    - path: "apps/frontend/src/components/player-seat.tsx"
      provides: "Individual player seat component"
      exports: ["PlayerSeat"]
  key_links:
    - from: "character-card.tsx"
      to: "globals.css"
      via: "Tailwind utility classes using @theme tokens"
      pattern: "character-duke|character-captain|character-assassin|character-ambassador|character-countess"
    - from: "player-seat.tsx"
      to: "lucide-react"
      via: "Coins and Shield icon imports"
      pattern: "import.*Coins.*Shield.*from.*lucide-react"
---

<objective>
Create the design foundation for the visual redesign: character color tokens in globals.css and two new reusable components (CharacterCard and PlayerSeat) that downstream plans will use.

Purpose: Establish the visual building blocks before any existing component is modified. CharacterCard and PlayerSeat are consumed by Plans 02 and 03.
Output: globals.css extended with 5 character colors, character-card.tsx (new), player-seat.tsx (new), unit tests for both.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-CONTEXT.md
@.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-RESEARCH.md

<interfaces>
<!-- Shared types consumed by new components -->

From packages/shared/src/types/card.ts:
```typescript
export enum CardType {
  DUKE = "DUKE",
  ASSASSIN = "ASSASSIN",
  CAPTAIN = "CAPTAIN",
  AMBASSADOR = "AMBASSADOR",
  CONTESSA = "CONTESSA",
}

export interface Card {
  type: CardType
  revealed: boolean
}
```

From packages/shared/src/types/game-state.ts:
```typescript
export interface PublicPlayerState {
  id: string
  name: string
  coins: number
  eliminated: boolean
  cardCount: number
  revealedCards: Card[]
}
```

From apps/frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character color tokens + CharacterCard component</name>
  <files>
    apps/frontend/src/app/globals.css,
    apps/frontend/src/components/character-card.tsx,
    apps/frontend/src/components/__tests__/character-card.test.tsx
  </files>
  <read_first>
    apps/frontend/src/app/globals.css,
    packages/shared/src/types/card.ts,
    apps/frontend/src/lib/utils.ts
  </read_first>
  <behavior>
    - Test 1: renders face-up Duke card with Crown icon and "Duque" label (check for text "Duque" and class containing "character-duke")
    - Test 2: renders face-up Captain card with Anchor icon and "Capitao" label
    - Test 3: renders face-up Assassin card with Sword icon and "Assassino" label
    - Test 4: renders face-up Ambassador card with Globe icon and "Embaixador" label
    - Test 5: renders face-up Countess card with Gem icon and "Condessa" label
    - Test 6: renders revealed/eliminated card with opacity-40 and grayscale classes
    - Test 7: renders card back (showFace=false) with "?" text and no character name
    - Test 8: renders different sizes (sm, md, lg) with appropriate dimension classes
  </behavior>
  <action>
1. Extend globals.css `@theme` block — append 5 character color tokens AFTER existing tokens (do NOT modify existing tokens):
   ```css
   --color-character-duke: hsl(271 76% 53%);
   --color-character-captain: hsl(217 91% 60%);
   --color-character-assassin: hsl(0 84% 60%);
   --color-character-ambassador: hsl(142 71% 45%);
   --color-character-countess: hsl(330 80% 65%);
   ```

2. Create `__tests__/character-card.test.tsx` with the behavior tests above. Use `@testing-library/react` render + screen.

3. Create `character-card.tsx`:
   - Import `Crown, Anchor, Sword, Globe, Gem, Shield` from lucide-react (Shield for card back)
   - Import `CardType` from `@coup/shared` and `cn` from `@/lib/utils`
   - Define `CHARACTER_CONFIG` record mapping each CardType to `{ label, colorClass, borderClass, Icon }`:
     - DUKE: label "Duque", colorClass "text-character-duke", borderClass "border-character-duke", Icon: Crown
     - CAPTAIN: label "Capitao", colorClass "text-character-captain", borderClass "border-character-captain", Icon: Anchor
     - ASSASSIN: label "Assassino", colorClass "text-character-assassin", borderClass "border-character-assassin", Icon: Sword
     - AMBASSADOR: label "Embaixador", colorClass "text-character-ambassador", borderClass "border-character-ambassador", Icon: Globe
     - CONTESSA: label "Condessa", colorClass "text-character-countess", borderClass "border-character-countess", Icon: Gem
   - Props: `type: CardType | string`, `revealed: boolean`, `showFace?: boolean` (defaults true), `size?: "sm" | "md" | "lg"` (defaults "md"), `className?: string`
   - When `showFace` is false: render card back with dark zinc bg, zinc-700 border, Shield icon in zinc-600, "?" text
   - When `showFace` is true: render face with bg-zinc-900, character border color (border-2), character icon, character label
   - When `revealed` is true AND showFace is true: add `opacity-40 grayscale` to indicate eliminated
   - Size classes: sm = "w-16 h-20 p-1.5", md = "w-20 h-28 p-3", lg = "w-24 h-32 p-4"
   - Export `CHARACTER_CONFIG` as a named export (consumed by action-bar in Plan 03)

4. Run tests, verify all pass.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/components/__tests__/character-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-character-duke` in @theme block
    - globals.css contains `--color-character-countess` in @theme block
    - character-card.tsx exports `CharacterCard` and `CHARACTER_CONFIG`
    - character-card.tsx imports `Crown, Anchor, Sword, Globe, Gem` from lucide-react
    - character-card.tsx contains `opacity-40` and `grayscale` for revealed cards
    - All 8 tests pass
  </acceptance_criteria>
  <done>
    CharacterCard component renders all 5 character types with correct colors, icons, and labels.
    Revealed cards show greyed-out treatment. Card back shows uniform dark pattern.
    5 character color tokens exist in globals.css @theme block.
    All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlayerSeat component</name>
  <files>
    apps/frontend/src/components/player-seat.tsx,
    apps/frontend/src/components/__tests__/player-seat.test.tsx
  </files>
  <read_first>
    packages/shared/src/types/game-state.ts,
    apps/frontend/src/components/player-panel.tsx,
    apps/frontend/src/lib/utils.ts
  </read_first>
  <behavior>
    - Test 1: renders player name and avatar initial (first letter uppercase)
    - Test 2: renders coin count with Coins icon present
    - Test 3: renders card count with Shield icon present
    - Test 4: active player has ring-2 class applied
    - Test 5: eliminated player has opacity-40 class and "Eliminado" badge text
    - Test 6: isMe player shows "(voce)" text
    - Test 7: disconnected player shows "(desconectado)" text
    - Test 8: renders revealed cards with CharacterCard components
  </behavior>
  <action>
1. Create `__tests__/player-seat.test.tsx` with the behavior tests above. Mock `CharacterCard` as a simple div for isolation. Use `@testing-library/react`.

2. Create `player-seat.tsx`:
   - Import `Coins, Shield` from lucide-react
   - Import `PublicPlayerState` from `@coup/shared`, `cn` from `@/lib/utils`
   - Import `CharacterCard` from `./character-card`
   - Import `Badge` from `@/components/ui/badge`
   - Props interface `PlayerSeatProps`:
     - `player: PublicPlayerState`
     - `isActive: boolean`
     - `isMe: boolean`
     - `isDisconnected: boolean`
   - Outer container: `cn("flex items-center gap-3 rounded-lg p-2.5 border border-zinc-800 bg-zinc-900/50", isActive && "ring-2 ring-white/80 bg-zinc-800/80", player.eliminated && "opacity-40", isMe && "border-zinc-600")`
   - Avatar circle: `div` with `h-9 w-9 rounded-full flex items-center justify-center text-sm font-bold shrink-0 bg-zinc-700 text-zinc-300`, active state adds `bg-white/10 text-white`; shows `player.name.charAt(0).toUpperCase()`
   - Center section (flex-1 min-w-0):
     - Name: `text-sm font-medium truncate`, with `line-through` if eliminated
     - Below name: if `isMe`, show `(voce)` in `text-xs text-muted-foreground`; if disconnected, show `(desconectado)` in `text-xs text-muted-foreground`
   - Right section (shrink-0, flex items-center gap-2):
     - Coins: `<Coins className="h-3.5 w-3.5 text-yellow-400" />` + `{player.coins}` in `text-xs text-yellow-400`
     - Cards: `<Shield className="h-3.5 w-3.5 text-zinc-400" />` + `{player.cardCount}` in `text-xs text-zinc-400`
   - Status badges (flex gap-1 below the main row):
     - Active: `<Badge className="bg-emerald-500 text-white text-[10px] px-1.5 py-0">Sua vez</Badge>`
     - Eliminated: `<Badge variant="outline" className="text-[10px] px-1.5 py-0 text-destructive border-destructive/30">Eliminado</Badge>`
   - Revealed cards row: if `player.revealedCards.length > 0`, render a flex row of `<CharacterCard type={card.type} revealed={true} showFace={true} size="sm" />` for each

3. Run tests, verify all pass.
  </action>
  <verify>
    <automated>cd apps/frontend && npx vitest run src/components/__tests__/player-seat.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - player-seat.tsx exports `PlayerSeat`
    - player-seat.tsx imports `Coins` and `Shield` from lucide-react
    - player-seat.tsx imports `CharacterCard` from `./character-card`
    - player-seat.tsx contains `ring-2` for active state
    - player-seat.tsx contains `opacity-40` for eliminated state
    - player-seat.tsx contains text "voce" and "desconectado"
    - player-seat.tsx contains text "Eliminado"
    - All 8 tests pass
  </acceptance_criteria>
  <done>
    PlayerSeat component renders player info as a card-like seat tile with avatar, coins (Coins icon), cards (Shield icon), active ring, eliminated dimming, and revealed cards using CharacterCard.
    All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates purely presentational components with no data input, no API calls, no user-submitted content rendering beyond player names (already sanitized server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | I (Info Disclosure) | CharacterCard | accept | Card type values come from server-projected state; no hidden info exposed by rendering face cards the player already owns |
</threat_model>

<verification>
1. `cd apps/frontend && npx vitest run` — all existing + new tests pass
2. `grep "character-duke" apps/frontend/src/app/globals.css` — token exists
3. `grep "export function CharacterCard" apps/frontend/src/components/character-card.tsx` — component exported
4. `grep "export function PlayerSeat" apps/frontend/src/components/player-seat.tsx` — component exported
</verification>

<success_criteria>
- 5 character color tokens in globals.css @theme block (duke, captain, assassin, ambassador, countess)
- CharacterCard renders all 5 types with correct icon + color + label; handles revealed and card-back states
- PlayerSeat renders player info with avatar, coins icon, card count icon, active ring, eliminated styling
- All unit tests pass (character-card + player-seat)
- No changes to existing components (those come in Plans 02 and 03)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-ui-redesign-visual-game-interface/06.1-01-SUMMARY.md`
</output>
