---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - ROOM-04
  - INIT-03
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "CI runs typecheck and vitest on push to main"
    - "All tests pass in CI"
    - "npm run dev starts both services and badge shows Conectado"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "tsc --noEmit"
      min_lines: 20
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run typecheck and npm run test"
      pattern: "npm run"
---

<objective>
Create the GitHub Actions CI workflow and perform final human verification of the complete Phase 1 deliverable: both services running, WebSocket connected, badge visible, UUID persistent.

Purpose: D-06 and D-07 require CI with typecheck + vitest on push to main. The checkpoint verifies the full Phase 1 success criteria end-to-end.
Output: CI workflow file, human-verified working foundation.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md (D-06: CI runs tsc --noEmit + vitest; D-07: push to main only),
    package.json (verify scripts.typecheck and scripts.test exist)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per D-06 and D-07:

    ```yaml
    name: CI

    on:
      push:
        branches: [main]

    jobs:
      check:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - uses: actions/setup-node@v4
            with:
              node-version: 24
              cache: npm

          - run: npm ci

          - name: Typecheck
            run: npm run typecheck

          - name: Test
            run: npm run test
    ```

    This satisfies:
    - D-06: CI runs `tsc --noEmit` (via npm run typecheck) + `vitest` (via npm run test)
    - D-07: CI triggers on push to main only (no PRs, no other branches)
  </action>
  <verify>
    <automated>cd C:/Users/julia/Documents/GitHub/Coup && cat .github/workflows/ci.yml | grep -q "push" && cat .github/workflows/ci.yml | grep -q "main" && cat .github/workflows/ci.yml | grep -q "typecheck" && cat .github/workflows/ci.yml | grep -q "test" && echo "CI workflow valid"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: .github/workflows/ci.yml
    - grep -q "push" .github/workflows/ci.yml
    - grep -q "main" .github/workflows/ci.yml
    - grep -q "npm run typecheck" .github/workflows/ci.yml
    - grep -q "npm run test" .github/workflows/ci.yml
    - grep -q "npm ci" .github/workflows/ci.yml
    - grep -q "node-version.*24" .github/workflows/ci.yml
    - No "pull_request" trigger present (D-07: main push only)
  </acceptance_criteria>
  <done>GitHub Actions CI workflow triggers on push to main, runs typecheck and tests per D-06/D-07</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 Foundation end-to-end</name>
  <files>none — verification only</files>
  <action>
    Human verifies the complete Phase 1 deliverable end-to-end. All automation is complete — this checkpoint confirms visual and functional correctness that cannot be tested automatically.

    What was built:
    1. Monorepo with npm workspaces (apps/frontend, apps/backend, packages/shared)
    2. Shared TypeScript types for the entire game domain
    3. Express + Socket.IO backend with PING/PONG handler
    4. projectStateForPlayer() tested pure function
    5. Next.js frontend with UUID session, WebSocket client, connection badge
    6. GitHub Actions CI workflow

    Steps to verify:
    1. Run `npm run dev` from repo root — both services should start
    2. Open http://localhost:3000 in the browser
    3. Verify the connection badge appears in the bottom-right corner showing "Conectado" (green dot)
    4. Open DevTools > Application > localStorage — confirm a `coup_player_id` key with a UUID value exists
    5. Refresh the page — confirm the same UUID is still in localStorage (ROOM-04)
    6. Stop the backend (Ctrl+C the backend process) — badge should change to "Desconectado" (red dot)
    7. Run `npm run typecheck` — should pass with no errors
    8. Run `npm run test` — all unit tests should pass
  </action>
  <verify>
    <automated>cd C:/Users/julia/Documents/GitHub/Coup && npm run typecheck && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - npm run dev starts both services without errors
    - Badge visible in bottom-right corner of http://localhost:3000
    - Badge shows "Conectado" with green dot when backend is running
    - Badge shows "Desconectado" with red dot when backend is stopped
    - localStorage contains coup_player_id with UUID value
    - Same UUID persists after page refresh
    - npm run typecheck exits 0
    - npm run test exits 0 with all tests passing
  </acceptance_criteria>
  <done>Human confirms all Phase 1 ROADMAP success criteria are met: both services run, UUID persists, WebSocket connects, badge works, tests pass, CI ready</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` exists with correct triggers and steps
- All Phase 1 success criteria verified by human:
  1. `npm run dev` starts both services
  2. UUID in localStorage survives refresh (ROOM-04)
  3. WebSocket connection confirmed via badge (SYNC-01)
  4. Badge shows correct state (D-05)
  5. All tests pass (including projectStateForPlayer — INIT-03, SYNC-02)
  6. CI workflow ready for first push to main
</verification>

<success_criteria>
- CI workflow file matches D-06 (typecheck + vitest) and D-07 (push to main only)
- Human confirms all Phase 1 ROADMAP success criteria are met
- All 5 success criteria from ROADMAP Phase 1 are verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
