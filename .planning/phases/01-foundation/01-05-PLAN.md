---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.workspace.ts
  - package.json
autonomous: true
gap_closure: true
requirements:
  - ROOM-04
  - INIT-03
  - SYNC-01
  - SYNC-02

must_haves:
  truths:
    - "`npm run test` exits 0 from the repo root"
    - "Root vitest discovers and runs all 10 tests (6 backend + 4 frontend)"
    - "Frontend `@/` path alias resolves correctly when tests run from the repo root"
  artifacts:
    - path: "vitest.workspace.ts"
      provides: "Root workspace config that delegates to per-app vitest configs"
      contains: "apps/backend/vitest.config.ts"
    - path: "package.json"
      provides: "Root `test` script delegates to vitest workspace"
      contains: "vitest run"
  key_links:
    - from: "vitest.workspace.ts"
      to: "apps/frontend/vitest.config.ts"
      via: "workspace entry"
      pattern: "apps/frontend/vitest.config.ts"
    - from: "vitest.workspace.ts"
      to: "apps/backend/vitest.config.ts"
      via: "workspace entry"
      pattern: "apps/backend/vitest.config.ts"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm run test → vitest workspace"
      pattern: "npm run test"
---

<objective>
Close the CI blocker: `npm run test` at the repo root fails because the root vitest process does not apply `apps/frontend/vitest.config.ts`, so the `@/lib/session` import alias is unresolved.

Purpose: Unblock CI — `.github/workflows/ci.yml` calls `npm run test` on every push to main. Without this fix every push will fail with ERR_MODULE_NOT_FOUND.
Output: A `vitest.workspace.ts` at the repo root that delegates to both per-app configs, ensuring each test suite runs with its own resolver, globals, and environment settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vitest.workspace.ts at repo root</name>
  <files>vitest.workspace.ts</files>

  <read_first>
    - `apps/frontend/vitest.config.ts` — confirm the file path and that it has the `@/` alias + `environment: "jsdom"`
    - `apps/backend/vitest.config.ts` — confirm the file path and `environment: "node"`
    - `package.json` (root) — confirm current `"test": "vitest run"` script
  </read_first>

  <action>
Create `/vitest.workspace.ts` at the repo root with this exact content:

```typescript
export default [
  "apps/backend/vitest.config.ts",
  "apps/frontend/vitest.config.ts",
]
```

This array syntax is the vitest workspace format. Vitest reads it automatically when a `vitest.workspace.ts` is present at the working directory. Each entry path is relative to the repo root. Each referenced config keeps its own `resolve.alias`, `environment`, `include` glob, and `globals` setting — no merging occurs.

Do NOT change the `package.json` `"test"` script. The existing `"vitest run"` command will pick up `vitest.workspace.ts` automatically when the file is present at the root.

Do NOT add a root-level `include` glob or `resolve.alias` to this file — the workspace file is an array only, not a `defineConfig` call. Adding config keys here overrides per-app configs and breaks the alias isolation.
  </action>

  <verify>
    <automated>cd /c/Users/julia/Documents/GitHub/Coup && npm run test</automated>
  </verify>

  <acceptance_criteria>
    - `npm run test` exits with code 0
    - Output contains "10 passed" (6 backend + 4 frontend)
    - No ERR_MODULE_NOT_FOUND error for `@/lib/session`
    - Output shows two test suites running: `apps/backend/src/__tests__/project-state.test.ts` and `apps/frontend/src/__tests__/session.test.ts`
    - Verify with: `npm run test 2>&1 | grep -E "passed|failed|ERR_MODULE"` — must show "10 passed", zero "failed", zero "ERR_MODULE"
  </acceptance_criteria>

  <done>File `vitest.workspace.ts` exists at repo root, `npm run test` exits 0 with all 10 tests passing, no module resolution errors.</done>
</task>

</tasks>

<verification>
Run from repo root:

```bash
npm run test
```

Expected output: 10 tests pass across 2 suites. Exit code 0.

Cross-check that per-app runs still work independently:

```bash
cd apps/backend && npx vitest run
cd apps/frontend && npx vitest run
```

Both must still exit 0 (workspace file must not break standalone app runs).
</verification>

<success_criteria>
- `npm run test` at repo root exits 0
- All 10 tests pass: 6 in `apps/backend`, 4 in `apps/frontend`
- `@/lib/session` resolves correctly in frontend tests (no ERR_MODULE_NOT_FOUND)
- Per-app vitest runs in `apps/backend` and `apps/frontend` still work independently
- CI unblocked: next push to main will pass the `npm run test` step in `.github/workflows/ci.yml`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
