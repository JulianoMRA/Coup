---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/backend/src/index.ts
  - apps/backend/src/socket-handler.ts
  - apps/backend/src/game/project-state.ts
  - apps/backend/src/__tests__/project-state.test.ts
autonomous: true
requirements:
  - INIT-03
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "projectStateForPlayer strips hidden cards for opponents"
    - "projectStateForPlayer preserves the requesting player's full hand"
    - "projectStateForPlayer exposes revealed cards for all players"
    - "Express + Socket.IO server starts and listens on port 3001"
    - "Socket.IO accepts connection with playerId in auth handshake"
  artifacts:
    - path: "apps/backend/src/game/project-state.ts"
      provides: "Pure function projectStateForPlayer(gameState, playerId) -> ClientGameState"
      exports: ["projectStateForPlayer"]
      min_lines: 20
    - path: "apps/backend/src/__tests__/project-state.test.ts"
      provides: "Unit tests for projectStateForPlayer covering INIT-03 and SYNC-02"
      min_lines: 50
    - path: "apps/backend/src/index.ts"
      provides: "Express + Socket.IO server bootstrap"
      exports: ["httpServer", "io"]
      min_lines: 15
    - path: "apps/backend/src/socket-handler.ts"
      provides: "Socket connection handler with PING/PONG and playerId extraction"
      exports: ["registerSocketHandlers"]
  key_links:
    - from: "apps/backend/src/game/project-state.ts"
      to: "@coup/shared"
      via: "import type { GameState, ClientGameState, PublicPlayerState }"
      pattern: "import.*@coup/shared"
    - from: "apps/backend/src/index.ts"
      to: "apps/backend/src/socket-handler.ts"
      via: "registerSocketHandlers(io)"
      pattern: "registerSocketHandlers"
---

<objective>
Implement the Express + Socket.IO backend server and the `projectStateForPlayer()` pure function using TDD. The filter function is the security core of the game — it MUST be tested before any broadcast code exists (per RESEARCH.md sequencing requirement).

Purpose: INIT-03 requires the server never sends hidden cards to wrong clients. SYNC-01/SYNC-02 require WebSocket transport with per-player state projection. This plan delivers both.
Output: Running backend server with Socket.IO, tested projectStateForPlayer function, PING/PONG handler for connection verification.
</objective>

<execution_context>
@C:/Users/julia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types from packages/shared that this plan depends on (created by Plan 01) -->

From packages/shared/src/types/card.ts:
```typescript
export enum CardType { DUKE, ASSASSIN, CAPTAIN, AMBASSADOR, CONTESSA }
export interface Card { type: CardType; revealed: boolean }
```

From packages/shared/src/types/game-state.ts:
```typescript
export interface PlayerState { id: string; name: string; coins: number; hand: Card[]; eliminated: boolean }
export interface PublicPlayerState { id: string; name: string; coins: number; eliminated: boolean; cardCount: number; revealedCards: Card[] }
export enum GamePhase { LOBBY, AWAITING_ACTION, ... , GAME_OVER }
export interface GameState { roomId: string; players: PlayerState[]; phase: GamePhase; activePlayerId: string; pendingAction: PendingAction | null; deck: Card[]; log: string[] }
export interface ClientGameState { myHand: Card[]; players: PublicPlayerState[]; phase: GamePhase; activePlayerId: string; pendingAction: PendingAction | null; log: string[] }
```

From packages/shared/src/types/events.ts:
```typescript
export interface ServerToClientEvents { STATE_UPDATE: (state: ClientGameState) => void; PONG: () => void; ERROR: (message: string) => void }
export interface ClientToServerEvents { PING: () => void }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD projectStateForPlayer — RED then GREEN</name>
  <files>
    apps/backend/src/__tests__/project-state.test.ts,
    apps/backend/src/game/project-state.ts
  </files>
  <read_first>
    apps/backend/src/__tests__/project-state.test.ts (existing stub from Plan 01 — replace .todo with real tests),
    packages/shared/src/types/game-state.ts (exact type signatures to use),
    packages/shared/src/types/card.ts (CardType enum values),
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: projectStateForPlayer example implementation)
  </read_first>
  <behavior>
    - Test: "strips hidden cards — opponent players have no hand field, only cardCount and revealedCards" (INIT-03)
    - Test: "preserves requesting player's full hand with card types and revealed status" (INIT-03)
    - Test: "exposes revealed cards for all players including opponents" (INIT-03)
    - Test: "returns correct cardCount excluding revealed cards" (SYNC-02)
    - Test: "returns empty hand array if playerId not found in game state"
    - Test: "preserves phase, activePlayerId, pendingAction, log from game state" (SYNC-02)
  </behavior>
  <action>
    **RED phase:** Replace the .todo stubs in project-state.test.ts with real test implementations. Create a test fixture `makeTestGameState(overrides?)` that builds a valid GameState with 2-3 players, each having 2 cards. Import `projectStateForPlayer` from `../game/project-state`. Run tests — they MUST fail (function doesn't exist yet). Commit: `test(01-02): add failing tests for projectStateForPlayer`.

    **GREEN phase:** Implement `projectStateForPlayer` in `apps/backend/src/game/project-state.ts` following the pattern from RESEARCH.md Pattern 4:
    - Import types from `@coup/shared`
    - Function signature: `(gameState: GameState, playerId: string): ClientGameState`
    - Find requesting player by playerId
    - Map all players to PublicPlayerState: { id, name, coins, eliminated, cardCount: hand.filter(c => !c.revealed).length, revealedCards: hand.filter(c => c.revealed) }
    - Return { myHand: requestingPlayer?.hand ?? [], players: publicPlayers, phase, activePlayerId, pendingAction, log }
    - CRITICAL: Never include `hand` in PublicPlayerState — only `cardCount` and `revealedCards`
    Run tests — they MUST pass. Commit: `feat(01-02): implement projectStateForPlayer`.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Documents/GitHub/Coup && npx vitest run apps/backend/src/__tests__/project-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function projectStateForPlayer" apps/backend/src/game/project-state.ts
    - grep -q "import.*@coup/shared" apps/backend/src/game/project-state.ts
    - grep -q "cardCount" apps/backend/src/game/project-state.ts
    - grep -q "revealedCards" apps/backend/src/game/project-state.ts
    - grep -qv "\.todo" apps/backend/src/__tests__/project-state.test.ts (no more todo stubs)
    - All 6+ tests pass in project-state.test.ts
    - No occurrence of "hand" in PublicPlayerState mapping (only cardCount and revealedCards)
  </acceptance_criteria>
  <done>projectStateForPlayer is implemented and all unit tests pass, covering card stripping (INIT-03), state projection (SYNC-02), and edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Express + Socket.IO server with PING/PONG handler</name>
  <files>
    apps/backend/src/index.ts,
    apps/backend/src/socket-handler.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Express + Socket.IO bootstrap, Pattern 3: auth handshake),
    packages/shared/src/types/events.ts (ServerToClientEvents, ClientToServerEvents),
    apps/backend/package.json (verify express, socket.io in deps)
  </read_first>
  <action>
    **apps/backend/src/index.ts:**
    - Import express, createServer from node:http, Server from socket.io
    - Create Express app, HTTP server, Socket.IO server with typed events (ServerToClientEvents, ClientToServerEvents)
    - CORS origin: `process.env.FRONTEND_URL ?? "http://localhost:3000"`
    - Import and call `registerSocketHandlers(io)`
    - Listen on `process.env.PORT ?? 3001`
    - Console.log: `Backend listening on :${PORT}`
    - Export httpServer and io for potential test use

    **apps/backend/src/socket-handler.ts:**
    - Import Server type from socket.io with typed events from @coup/shared
    - Export function `registerSocketHandlers(io: Server<ClientToServerEvents, ServerToClientEvents>)`
    - On "connection": extract `playerId` from `socket.handshake.auth.playerId`
    - If no playerId, disconnect with error "Missing playerId in auth"
    - Log: `Player connected: ${playerId}`
    - Register PING handler that emits PONG back to the same socket
    - On "disconnect": log `Player disconnected: ${playerId}`

    Verify the server starts with `npm run dev --workspace=apps/backend` and logs the listening message.
  </action>
  <verify>
    <automated>cd C:/Users/julia/Documents/GitHub/Coup && npx tsx apps/backend/src/index.ts &amp; SERVER_PID=$!; sleep 2; curl -s http://localhost:3001 > /dev/null; RESULT=$?; kill $SERVER_PID 2>/dev/null; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "express" apps/backend/src/index.ts
    - grep -q "createServer" apps/backend/src/index.ts
    - grep -q "socket.io" apps/backend/src/index.ts
    - grep -q "FRONTEND_URL" apps/backend/src/index.ts
    - grep -q "3001" apps/backend/src/index.ts
    - grep -q "registerSocketHandlers" apps/backend/src/socket-handler.ts
    - grep -q "playerId" apps/backend/src/socket-handler.ts
    - grep -q "PING" apps/backend/src/socket-handler.ts
    - grep -q "PONG" apps/backend/src/socket-handler.ts
    - grep -q "@coup/shared" apps/backend/src/socket-handler.ts
    - Server starts without TypeScript compilation errors
  </acceptance_criteria>
  <done>Express + Socket.IO server starts on port 3001, accepts connections with playerId auth, responds to PING with PONG, logs connections and disconnections</done>
</task>

</tasks>

<verification>
- All projectStateForPlayer unit tests pass: `npx vitest run apps/backend/src/__tests__/project-state.test.ts`
- Backend server starts without errors: `npm run dev --workspace=apps/backend`
- TypeScript compiles: `tsc --noEmit -p apps/backend/tsconfig.json`
</verification>

<success_criteria>
- projectStateForPlayer correctly strips hidden cards (INIT-03 verified by tests)
- projectStateForPlayer returns per-player projection (SYNC-02 verified by tests)
- Express + Socket.IO server accepts WebSocket connections (SYNC-01 infrastructure)
- PING/PONG handler provides connection health verification mechanism
- All tests written before implementation (TDD red-green cycle)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
