describe("getOrCreatePlayerId", () => {
  it.todo("returns existing UUID from localStorage")
  it.todo("generates and persists new UUID when none exists")
  it.todo("returns empty string during SSR (no window)")
})
